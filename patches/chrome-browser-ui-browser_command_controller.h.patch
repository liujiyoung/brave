diff --git a/chrome/browser/ui/browser_command_controller.h b/chrome/browser/ui/browser_command_controller.h
index 985d65e895a9ffd5e4011a9e21fa96c7da2dc22f..351ba0cde2434656057b31debd92a51f35390ecf 100644
--- a/chrome/browser/ui/browser_command_controller.h
+++ b/chrome/browser/ui/browser_command_controller.h
@@ -120,6 +120,7 @@ class BrowserCommandController : public CommandUpdater,
       Profile* profile);
 
  private:
+  friend class BraveBrowserCommandController;
 #if BUILDFLAG(IS_CHROMEOS)
   friend class BrowserCommandControllerBrowserTestLockedFullscreen;
 #endif
@@ -196,7 +197,7 @@ class BrowserCommandController : public CommandUpdater,
 
   // Update commands whose state depends on the type of fullscreen mode the
   // window is in.
-  void UpdateCommandsForFullscreenMode();
+  virtual void UpdateCommandsForFullscreenMode();
 
   // Update commands whose state depends on whether they're available to hosted
   // app windows.

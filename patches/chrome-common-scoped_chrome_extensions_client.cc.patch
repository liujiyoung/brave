diff --git a/chrome/common/scoped_chrome_extensions_client.cc b/chrome/common/scoped_chrome_extensions_client.cc
index bf55ba3d757daf2c917a24b5be0e06e5b8353d51..0a1f57e6267a9ead38b48ad1023bed9b22adc1f6 100644
--- a/chrome/common/scoped_chrome_extensions_client.cc
+++ b/chrome/common/scoped_chrome_extensions_client.cc
@@ -61,7 +61,11 @@ CombineAllAvailabilityCheckMaps() {
 }  // namespace
 
 ScopedChromeExtensionsClient::ScopedChromeExtensionsClient()
-    : client_(std::make_unique<ChromeExtensionsClient>()) {
+    : client_(std::make_unique<BraveExtensionsClient>()) {
+  if (!ClaimProcessWideClient(this)) {
+    client_.reset();
+    return;
+  }
   client_->SetFeatureDelegatedAvailabilityCheckMap(
       CombineAllAvailabilityCheckMaps());
 #if BUILDFLAG(ENABLE_PLATFORM_APPS)
@@ -82,6 +86,9 @@ ScopedChromeExtensionsClient::ScopedChromeExtensionsClient()
 }
 
 ScopedChromeExtensionsClient::~ScopedChromeExtensionsClient() {
+  if (!ReleaseProcessWideClient(this)) {
+    return;
+  }
   DCHECK_EQ(ExtensionsClient::Get(), client_.get());
   ExtensionsClient::Set(nullptr);
 }

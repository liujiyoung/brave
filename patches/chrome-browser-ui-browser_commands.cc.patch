diff --git a/chrome/browser/ui/browser_commands.cc b/chrome/browser/ui/browser_commands.cc
index 83fc259bb09970e6d4e43913a6c15121275b6407..2d26250e9254a3db8045c4b4a9cb2ad1054a3089 100644
--- a/chrome/browser/ui/browser_commands.cc
+++ b/chrome/browser/ui/browser_commands.cc
@@ -500,7 +500,12 @@ void MoveTabsToWindowImpl(BrowserWindowInterface* source,
     } else if (auto collection =
                    std::get_if<std::unique_ptr<DetachedTabCollection>>(
                        &tab_or_collection)) {
-      if (std::holds_alternative<std::unique_ptr<tabs::TabGroupTabCollection>>(
+      if (std::holds_alternative<
+              std::unique_ptr<tabs::TreeTabNodeTabCollection>>(
+              collection->get()->collection_)) {
+        target_model->InsertDetachedTreeTabNodeAt(std::move(*collection),
+                                                  target_model->count());
+      } else if (std::holds_alternative<std::unique_ptr<tabs::TabGroupTabCollection>>(
               collection->get()->collection_)) {
         target_model->InsertDetachedTabGroupAt(std::move(*collection),
                                                target_model->count());
@@ -736,6 +741,11 @@ void ReloadInternal(BrowserWindowInterface* browser,
   bool multiple_ui_tabs_selected =
       active_tab && tab_strip_model->selection_model().size() >
                         (active_tab->IsSplit() ? 2 : 1);
+  if (multiple_ui_tabs_selected &&
+      static_cast<BraveTabStripModel*>(tab_strip_model)
+          ->IsOnlyActiveTabAndTreeDescendantsSelected()) {
+    multiple_ui_tabs_selected = false;
+  }
 
   if (multiple_ui_tabs_selected) {
     // Reloading a tab may change the selection (see crbug.com/339061099), so
@@ -1167,6 +1177,7 @@ void Reload(BrowserWindowInterface* browser,
 
 void ReloadBypassingCache(BrowserWindowInterface* browser,
                           WindowOpenDisposition disposition) {
+  if (BraveMaybeHandleReloadBypassingCache(browser)) return;
   base::RecordAction(UserMetricsAction("ReloadBypassingCache"));
   ReloadInternal(browser, disposition, true);
 }
@@ -1467,7 +1478,9 @@ void CloseTab(BrowserWindowInterface* browser) {
       active_tab->IsSplit() &&
       browser->GetTabStripModel()->selection_model().size() == 2;
 
-  if (only_active_split_tab_selected) {
+  if (only_active_split_tab_selected ||
+      static_cast<BraveTabStripModel*>(browser->GetTabStripModel())
+          ->IsOnlyActiveTabAndTreeDescendantsSelected()) {
     RecordTabCloseCount(1);
     active_tab->GetContents()->Close();
     return;
@@ -1487,6 +1500,7 @@ void CloseTab(BrowserWindowInterface* browser) {
 #endif
 
   ToastController* toast_controller = ToastController::From(browser);
+  toast_controller = nullptr;
   if (!toast_controller) {
     CloseSelectedTabAndRecordTabCountMetric(browser);
     return;
@@ -1813,7 +1827,7 @@ void NewSplitTab(BrowserWindowInterface* browser,
   const GURL new_tab_url =
       !browser->GetProfile()->IsPrimaryOTRProfileWithRegularParent() &&
               tab_strip_model->count() > 1
-          ? GURL(chrome::kChromeUISplitViewNewTabPageURL)
+          ? GURL(chrome::kChromeUINewTabURL)
           : chrome::ChromeUINewTabURLAsGURL();
   tab_strip_model->delegate()->AddTabAt(
       new_tab_url, active_index + 1, true,
